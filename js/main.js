// Asking user's name

const firstName = prompt ('What is your name?');

// Asking user's last-name

const lastName = prompt ('What is your surname?');

// Asking user's favorite color

const favCol = prompt ('What is your favorite color?')

// Show result

document.getElementById('result').innerHTML = 'Your password is ' + firstName + lastName + favCol + '21';